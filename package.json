{
	"name": "@shopwp/wp-ui",
	"version": "0.0.1",
	"description": "An attempt to standardize a new WordPress UI for plugin admin pages",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"deploy": "npm publish --access=public"
	},
	"author": "Andrew Robbins",
	"license": "ISC",
	"devDependencies": {
		"@emotion/babel-plugin": "^11.10.5",
		"@emotion/css": "^11.10.5",
		"@emotion/react": "^11.10.5"
	}
}
